<% if prompts %>
  <% prompts.each do |p| %>
    <p><%= link_to p.text, prompt_drafts_path(prompt_id: p.id, user_id: p.user) %></p>
    <%= link_to "Delete", prompt_path(id: p.id, user_id: current_user.id), method: :delete %>
  <% end %>
<% end %>
