<% @prompts.each do |p| %>
  <p><%= link_to p.text, user_prompt_drafts_path(prompt_id: p.id) %></p>
  <%= link_to "Delete", user_prompt_path(id: p.id, user_id: current_user.id), method: :delete %>
<% end %>
